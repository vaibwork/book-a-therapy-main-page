# Main_Page — LOCAL DEV portal destinations (used by `npm run dev`).
# /patient, /practitioner, /clinic redirect to your locally running portal apps.

VITE_CUSTOMER_URL=http://localhost:3000
VITE_PRACTITIONER_URL=http://localhost:3002
VITE_CLINIC_URL=http://localhost:3001
